import 'package:flutter/cupertino.dart';

class BoatesScreen extends StatelessWidget {
  const BoatesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
