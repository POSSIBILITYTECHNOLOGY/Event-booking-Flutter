import 'package:flutter/cupertino.dart';

class DinnersScreen extends StatelessWidget {
  const DinnersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
